- set_title "#{@doc_collection.name} docs are being generated."

.container

  .page-header
    %h1
      = @doc_collection.name
    %p.lead
      These docs are being generated right now.
      %br
      Depending on how busy the servers are,
      %br
      it might take from a couple of minutes up to an hour.
      %br
      Refresh at will or enter your email address below
      %br
      to be notified when they are done.

  = render 'email_notifications/form', email_notification: @email_notification
